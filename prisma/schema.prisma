generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Post {
  id           Int       @id @default(autoincrement())
  title        String    @db.VarChar(255)
  details      String?
  deadline     DateTime?
  status       String    @default("未完了") @db.VarChar(20)
  completed_at DateTime?
}
